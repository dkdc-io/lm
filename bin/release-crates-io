#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

echo "Publishing dkdc-lm to crates.io..."
cargo publish -p dkdc-lm

echo "Publishing dkdc-lm-cli to crates.io..."
cargo publish -p dkdc-lm-cli

echo "Published!"
